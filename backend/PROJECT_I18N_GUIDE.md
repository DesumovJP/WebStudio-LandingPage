# 🌍 Керівництво: Англійські версії проектів (i18n)

## ✅ Що зроблено

1. **Увімкнено локалізацію для Project** в `schema.json`:
   ```json
   "pluginOptions": {
     "i18n": {
       "localized": true
     }
   }
   ```

2. **Оновлено `getProjects()`** для підтримки locale:
   - Тепер приймає параметр `locale: string` ('uk' або 'en')
   - GraphQL запит фільтрує проекти по locale
   - Автоматично використовує правильну мову в залежності від URL

3. **Оновлено `layout.tsx`**:
   - Передає поточний `locale` в `getProjects(locale)`
   - Українська версія (`/uk/`) показує українські проекти
   - Англійська версія (`/en/`) показує англійські проекти

---

## 📝 Як створити англійську версію проекту

### Варіант 1: Через Strapi Admin (Рекомендовано)

1. **Відкрийте Strapi Admin**: `https://webstudio-landingpage-production.up.railway.app/admin`

2. **Створіть або відкрийте проект**:
   - Content Manager → Project → [Ваш проект]

3. **Додайте локалізацію**:
   - В правому верхньому куті знайдіть **"Add localization"** або **"🌍"** іконку
   - Натисніть **"Add new locale"**
   - Виберіть **"English (en)"**
   - Натисніть **"Finish"**

4. **Заповніть англійські поля**:
   - `title` - англійська назва
   - `subtitle` - англійський підзаголовок
   - `description` - англійський опис
   - `metric` - англійська метрика
   - `stack` - технології (можна залишити як є)
   - `done` - що зроблено (англійською)
   - `benefits` - переваги (англійською)
   - `outcome` - результат (англійською)

5. **Опублікуйте**:
   - Натисніть **"Save"**
   - Натисніть **"Publish"**

### Варіант 2: Створити окремий entry для англійської

Якщо локалізація не працює, можна створити окремий проект:

1. **Створіть новий проект**:
   - Content Manager → Project → Create new entry

2. **Заповніть англійські поля**:
   - Всі поля англійською мовою
   - `title` має бути унікальним (не можна використовувати той самий title)

3. **Опублікуйте**

**⚠️ Увага**: Цей варіант не рекомендується, бо проекти не будуть пов'язані між собою.

---

## 🔍 Перевірка

### 1. Перевірте українську версію:
```
https://webbie-tau.vercel.app/uk/
```
Має показувати проекти з `locale: "uk"`

### 2. Перевірте англійську версію:
```
https://webbie-tau.vercel.app/en/
```
Має показувати проекти з `locale: "en"`

---

## 🛠️ Технічні деталі

### GraphQL запит з locale:
```graphql
query GetProjects {
  projects(locale: "en") {
    data {
      id
      attributes {
        title
        subtitle
        description
        locale
        ...
      }
    }
  }
}
```

### Код в `getProjects.ts`:
```typescript
export async function getProjects(locale: string = 'uk'): Promise<StrapiProject[]> {
  // ...
  body: JSON.stringify({
    query: PROJECTS_QUERY(locale), // Передаємо locale
  }),
  // ...
}
```

### Код в `layout.tsx`:
```typescript
const locale = await getValidatedLocale(resolvedParams);
const strapiProjects = await getProjects(locale); // Передаємо поточний locale
```

---

## ⚠️ Важливо

1. **Після увімкнення i18n**:
   - Потрібно перезапустити Strapi в development mode (якщо ще не зроблено)
   - Або зачекати, поки Railway синхронізує зміни

2. **Унікальність title**:
   - `unique: false` встановлено, бо з локалізацією один title може бути в різних локалях
   - Переконайтеся, що title унікальний в межах однієї локалі

3. **Медіа файли**:
   - `gallery` та `mainImage` **не локалізуються** (спільні для всіх локалей)
   - Якщо потрібні різні зображення для різних мов, створіть окремі entries

4. **JSON поля**:
   - `stack`, `done`, `benefits` - це JSON поля
   - Вони можуть містити масиви рядків
   - Для англійської версії заповніть їх англійською

---

## 📚 Додаткова інформація

- [Strapi i18n Documentation](https://docs.strapi.io/dev-docs/plugins/i18n)
- [GraphQL Locale Filtering](https://docs.strapi.io/dev-docs/plugins/graphql#locale-filtering)

