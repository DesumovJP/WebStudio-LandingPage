# 🔧 Глибока діагностика: Локалізація не зберігається в Strapi

## ⚠️ Проблема

Текст змінюється правильно, але при перемиканні локалей показується неправильний текст.

---

## 🔍 Можливі причини

### 1. Strapi кешує дані

**Рішення:**
1. **Перезапустіть Strapi на Railway**
   - Railway → ваш проект → Deployments → Redeploy
   - Або змініть будь-яку змінну середовища і redeploy

2. **Очистіть кеш Strapi локально** (якщо тестуєте локально):
   ```bash
   cd backend
   rm -rf .cache
   rm -rf build
   npm run develop
   ```

### 2. Проблема з базою даних

**Перевірка:**
1. Підключіться до PostgreSQL в Railway
2. Перевірте таблицю `projects`:
   ```sql
   SELECT id, locale, title FROM projects;
   ```
3. Перевірте, чи є записи з різними locale

**Якщо всі записи мають один locale:**
- Проблема в тому, як Strapi зберігає локалізовані дані
- Можливо, потрібно перестворити проект

### 3. Проблема з i18n plugin

**Перевірка:**
1. Settings → Internationalization → Locales
2. Перевірте, чи обидві локалі активні:
   - Ukrainian (uk) - Default ✅
   - English (en) ✅

**Якщо одна з локалей неактивна:**
- Активуйте її
- Перезапустіть Strapi

### 4. Проблема з пов'язанням локалей

**Перевірка:**
1. Відкрийте проект в Strapi
2. Перевірте, чи правильно пов'язані локалі
3. Можливо, потрібно перестворити зв'язок

---

## ✅ Рішення: Повне перестворення проекту

### Крок 1: Експортуйте дані (якщо потрібно)

1. **Content Manager → Project → [Ваш проект]**
2. **Скопіюйте всі дані** (текст, зображення тощо)
3. **Запишіть, які зображення використовуються**

### Крок 2: Видаліть проект

1. **Content Manager → Project → [Ваш проект]**
2. **Натисніть "Delete"**
3. **Підтвердіть видалення**

### Крок 3: Створіть проект заново

#### 3.1. Створіть українську версію (default)

1. **Content Manager → Project → Create new entry**
2. **Локаль**: Ukrainian (uk) - має бути default
3. **Заповніть ВСІ поля українською**
4. **Save → Publish**

#### 3.2. Додайте англійську локалізацію

1. **Відкрийте створений проект**
2. **Натисніть "Add localization"**
3. **Виберіть "English (en)"**
4. **Натисніть "Finish"**

**Важливо**: Після "Finish" ви побачите ПОРОЖНЮ форму (не копію української)!

5. **Заповніть ВСІ поля англійською** (з самого початку, не копіюйте)
6. **Save → Publish**

---

## 🔧 Альтернативне рішення: Перезапуск Strapi

### Крок 1: Перезапустіть Strapi на Railway

1. **Railway → ваш проект → Deployments**
2. **Натисніть "Redeploy"**

Або:

1. **Railway → Settings → Variables**
2. **Додайте/змініть будь-яку змінну** (наприклад, додайте пробіл)
3. **Save** - Railway автоматично перезапустить

### Крок 2: Очистіть кеш браузера

1. **Hard refresh**: Ctrl+Shift+R / Cmd+Shift+R
2. Або очистіть кеш браузера повністю

### Крок 3: Перевірте знову

1. Відкрийте Strapi Admin
2. Перевірте локалі

---

## 🛠️ Діагностика через GraphQL

### Перевірте, що повертає GraphQL:

1. Відкрийте: `https://webstudio-landingpage-production.up.railway.app/graphql`

2. Запит для української:
   ```graphql
   query {
     projects(locale: "uk") {
       data {
         id
         attributes {
           locale
           title
           subtitle
         }
       }
     }
   }
   ```

3. Запит для англійської:
   ```graphql
   query {
     projects(locale: "en") {
       data {
         id
         attributes {
           locale
           title
           subtitle
         }
       }
     }
   }
   ```

**Якщо обидва запити повертають однакові дані:**
- Проблема в Strapi i18n
- Потрібно перестворити проект

**Якщо повертають різні дані:**
- Проблема в frontend або кешуванні
- Перевірте frontend код

---

## 🔍 Перевірка в БД (якщо маєте доступ)

### Підключіться до PostgreSQL:

```sql
-- Перевірте всі проекти
SELECT id, locale, title, "publishedAt" 
FROM projects 
ORDER BY id, locale;

-- Перевірте, чи є проекти з обома локалями
SELECT locale, COUNT(*) 
FROM projects 
GROUP BY locale;
```

**Очікуваний результат:**
- Має бути 2 записи для кожного проекту (uk та en)
- Кожна з різним `locale` та `title`

---

## ⚠️ Якщо нічого не допомагає

### Останній варіант: Перестворити проект з нуля

1. **Видаліть проект повністю**
2. **Перезапустіть Strapi**
3. **Створіть проект заново**:
   - Спочатку українську (default)
   - Потім додайте англійську через "Add localization"
   - Заповніть обидві локалі правильно

---

## 📋 Чеклист діагностики

- [ ] Перезапущено Strapi на Railway
- [ ] Очищено кеш браузера
- [ ] Перевірено GraphQL Playground (обидва запити)
- [ ] Перевірено налаштування i18n в Strapi
- [ ] Перевірено БД (якщо маєте доступ)
- [ ] Перестворено проект (якщо нічого не допомагає)

