# 🔄 Синхронізація i18n локалізації на Railway

## ⚠️ Чому pre-deploy НЕ працює

`npm run develop` - це **довготривала команда**, яка:
- Запускає Strapi сервер
- Працює **нескінченно** (не завершується)
- Блокує deploy процес

**Pre-deploy команди мають завершитися**, інакше deploy не продовжиться.

---

## ✅ Правильні варіанти

### Варіант 1: Тимчасово змінити Start Command (Рекомендовано)

1. **Railway → Settings → Deploy → Start Command**
2. **Змініть на**: `npm run develop`
3. **Railway → Settings → Variables**:
   ```
   NODE_ENV=development
   ```
4. **Redeploy** проект
5. **Дочекайтеся синхронізації** (1-2 хвилини)
6. **Поверніть назад**:
   - Start Command: `npm start`
   - NODE_ENV: `production`
7. **Redeploy** знову

### Варіант 2: Використати Build Command (Вже налаштовано)

В `railway.json` вже є:
```json
{
  "build": {
    "buildCommand": "npm run build"
  }
}
```

`strapi build` синхронізує `schema.json` файли з БД під час build.

**Але**: Для i18n змін може знадобитися development mode.

### Варіант 3: Використати Railway Shell

1. **Railway → ваш проект → Shell**
2. Виконайте:
   ```bash
   cd backend
   NODE_ENV=development npm run develop
   ```
3. Дочекайтеся синхронізації (1-2 хвилини)
4. Натисніть `Ctrl+C` для зупинки
5. Redeploy проект

---

## 🎯 Рекомендований Workflow для i18n

### Крок 1: Перевірте, що файли в git

```bash
git ls-files | grep schema.json
# Має бути: backend/src/api/project/content-types/project/schema.json
```

### Крок 2: Тимчасово переключіть Railway в development mode

1. **Start Command**: `npm run develop`
2. **NODE_ENV**: `development`
3. **Redeploy**

### Крок 3: Дочекайтеся синхронізації

- Railway logs покажуть: `"Synchronizing schema..."` або `"Content types synced"`
- Зазвичай 1-2 хвилини

### Крок 4: Перевірте в адмін-панелі

1. Відкрийте: `https://webstudio-landingpage-production.up.railway.app/admin`
2. Content Manager → Project
3. Має з'явитися кнопка **"Add localization"** (🌍)

### Крок 5: Поверніть production mode

1. **Start Command**: `npm start`
2. **NODE_ENV**: `production`
3. **Redeploy**

**Важливо**: Зміни в БД залишаться навіть після повернення в production mode!

---

## 📋 Чеклист

- [ ] Файли `schema.json` закомічені в git
- [ ] Railway переключено в development mode
- [ ] Redeploy виконано
- [ ] Дочекалися синхронізації (1-2 хвилини)
- [ ] Перевірили в адмін-панелі (кнопка локалізації з'явилася)
- [ ] Повернули production mode
- [ ] Redeploy виконано

---

## 🔍 Перевірка синхронізації

### В Railway Logs шукайте:

```
[INFO] Synchronizing schema...
[INFO] Content types synced
[INFO] i18n plugin initialized
```

### В адмін-панелі:

1. Content Manager → Project → [Ваш проект]
2. Має бути кнопка **"Add localization"** (🌍) в правому верхньому куті
3. Або в списку проектів має бути колонка **"Locale"**

---

## ⚠️ Важливо

1. **Pre-deploy команди** мають завершитися - не використовуйте `npm run develop` там
2. **Build Command** (`npm run build`) синхронізує структуру, але для i18n може знадобитися development mode
3. **Після синхронізації** зміни залишаються в БД навіть в production mode
4. **Не забудьте** повернути production mode після синхронізації

