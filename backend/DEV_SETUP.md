# ✅ Налаштування окремої PostgreSQL бази для Dev

## 🎯 Правильний підхід: Окрема база для Dev

Це **безпечно** і **рекомендовано** для розробки.

---

## 🚀 Як створити окрему PostgreSQL базу на Railway

### Крок 1: Створи новий проект на Railway (або використай існуючий)

1. Зайди на [railway.app](https://railway.app)
2. Увійди через GitHub
3. **"New Project"** → **"Empty Project"** (або використай існуючий проект)

### Крок 2: Додай PostgreSQL базу

1. В проекті: **"New"** → **"Database"** → **"PostgreSQL"**
2. Railway автоматично створить базу даних
3. Назви базу, наприклад: `webstudio-dev-db` (для зручності)

### Крок 3: Отримай DATABASE_URL

1. Відкрий створений **PostgreSQL** сервіс
2. Перейди в **Variables** (або **Settings → Variables**)
3. Знайди `DATABASE_URL` або `POSTGRES_URL`
4. Скопіюй connection string (виглядає як `postgresql://user:password@host:5432/database`)

### Крок 4: Додай DATABASE_URL до `.env` файлу

Відкрий `backend/.env` і додай:

```env
NODE_ENV=development
DATABASE_URL=postgresql://user:password@host:5432/database
DATABASE_SSL=true
```

**Приклад:**
```env
NODE_ENV=development
DATABASE_URL=postgresql://postgres:password@containers-us-west-xxx.railway.app:5432/railway
DATABASE_SSL=true
```

### Крок 5: Запусти backend

```bash
cd backend
npm run dev
```

---

## ✅ Перевірка

Після запуску `npm run dev` ти побачиш в логах:
```
[INFO] Using PostgreSQL database: postgresql://...
```

---

## 🔒 Безпека

- ✅ Dev база **окрема** від production
- ✅ Можна безпечно тестувати міграції
- ✅ Можна видаляти/змінювати дані без ризику для production
- ✅ Production дані завжди в безпеці

---

## 📋 Структура баз даних

```
Development:
└── Railway: webstudio-dev-db (окрема база)

Production:
└── Railway: webstudio-production-db (production база)
```

---

## 🎯 Переваги

- ✅ **Безпека**: production дані завжди в безпеці
- ✅ **Гнучкість**: можна тестувати будь-які зміни
- ✅ **Швидкість**: не потрібно турбуватись про production
- ✅ **Командна робота**: кожен розробник має свою базу

---

**Готово!** Тепер ти маєш окрему базу для dev і можеш безпечно розробляти. 🎉
