# 🚨 Термінове виправлення: Локалізація не зберігається

## ⚠️ Проблема

Текст змінюється правильно, але при перемиканні локалей показується неправильний текст.

---

## 🔧 Швидке рішення

### Крок 1: Перезапустіть Strapi на Railway

**Це найчастіша причина!**

1. **Railway → ваш проект → Deployments**
2. **Натисніть "Redeploy"**

Або:

1. **Railway → Settings → Variables**
2. **Додайте пробіл до будь-якої змінної** (наприклад, `NODE_ENV=production ` → `NODE_ENV=production`)
3. **Save** - Railway автоматично перезапустить

**Дочекайтеся повного перезапуску** (1-2 хвилини)

### Крок 2: Перевірте GraphQL Playground

1. Відкрийте: `https://webstudio-landingpage-production.up.railway.app/graphql`

2. Запит для української:
   ```graphql
   query {
     projects(locale: "uk") {
       data {
         id
         attributes {
           locale
           title
         }
       }
     }
   }
   ```

3. Запит для англійської:
   ```graphql
   query {
     projects(locale: "en") {
       data {
         id
         attributes {
           locale
           title
         }
       }
     }
   }
   ```

**Що перевірити:**
- Чи повертаються різні `title` для різних locale?
- Чи правильний `locale` в attributes?

**Якщо обидва запити повертають однакові дані:**
→ Проблема в Strapi, потрібно перестворити проект

**Якщо повертають різні дані:**
→ Проблема в кешуванні або frontend

---

## 🛠️ Якщо перезапуск не допоміг

### Варіант 1: Перестворити проект

1. **Скопіюйте всі дані** (текст, зображення)
2. **Видаліть проект** в Strapi
3. **Створіть заново**:
   - Спочатку українську (default)
   - Потім додайте англійську через "Add localization"
   - Заповніть обидві локалі правильно

### Варіант 2: Перевірте БД напряму

Якщо маєте доступ до PostgreSQL:

```sql
-- Перевірте всі проекти
SELECT id, locale, title, "publishedAt" 
FROM projects 
ORDER BY id, locale;
```

**Очікуваний результат:**
- Має бути 2 записи для кожного проекту (uk та en)
- Кожна з різним `locale` та `title`

**Якщо всі записи мають один locale:**
→ Проблема в Strapi i18n, потрібно перестворити

---

## 🔍 Діагностика

### Перевірте в Strapi Admin:

1. **Відкрийте проект**
2. **Перемикніться на Ukrainian (uk)**
3. **Подивіться на URL в браузері** - має бути `?locale=uk`
4. **Перевірте поля** - чи там український текст?

5. **Перемикніться на English (en)**
6. **Подивіться на URL** - має бути `?locale=en`
7. **Перевірте поля** - чи там англійський текст?

**Якщо URL не змінюється:**
→ Проблема з перемиканням локалей в Strapi UI

**Якщо URL змінюється, але дані однакові:**
→ Проблема з збереженням даних в БД

---

## ⚡ Найшвидше рішення

1. **Перезапустіть Strapi** (Redeploy на Railway)
2. **Очистіть кеш браузера** (Ctrl+Shift+R)
3. **Перевірте GraphQL Playground**
4. **Якщо не допомогло - перестворіть проект**

---

## 📞 Що мені потрібно від вас

1. **Що показує GraphQL Playground?**
   - Чи повертаються різні дані для `locale: "uk"` та `locale: "en"`?

2. **Що показує URL в Strapi Admin?**
   - Чи змінюється `?locale=uk` на `?locale=en` при перемиканні?

3. **Чи перезапускали Strapi?**
   - Якщо ні - спробуйте зараз

